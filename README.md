# simple_flutter_app

Projet Flutter minimal.

## Prérequis

- Flutter SDK installé et disponible dans le PATH

## Démarrage

```bash
flutter pub get
flutter run
```

Si les dossiers de plateformes ne sont pas encore présents (android, ios, web, windows, macos, linux), exécute:

```bash
flutter create .
```
